# Frontend build-time config (Vite).
# Local dev: leave UNSET — the Vite proxy forwards /api → http://localhost:4000.
# Production (Vercel): set this to your deployed backend origin (NO trailing /api):
# VITE_API_BASE=https://cgd-api.onrender.com
